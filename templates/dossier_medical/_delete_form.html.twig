<form class= "mx-auto col-4 mb-4" method="post" action="{{ path('app_dossier_medical_delete', {'id': dossier_medical.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dossier_medical.id) }}">
    <button class="btn btn-outline-dark btn-primary">Supprimer Dossier</button>
</form>
