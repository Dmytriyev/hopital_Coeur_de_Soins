{% extends 'base.html.twig' %}

{% block title %}Ajouter Prescription{% endblock %}

{% block body %}
    <h1>Ajouter une Prescription</h1>

    {{ include('prescription/_form.html.twig') }}
	<a class="btn btn-outline-dark btn-primary mb-3" href="{{ path('app_dossier_medical_index') }}">Retour à la liste</a>

    {# <a class="btn btn-outline-dark btn-primary mb-3"  href="{{ path('app_prescription_index') }}">Retour à la liste</a> #}
{% endblock %}
