{% extends 'base.html.twig' %}

{% block title %}Consultation{% endblock %}

{% block body %}
    <h1>Consultation</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ consultation.id }}</td>
            </tr> #}
            <tr>
                <th>Date de consultation</th>
                <td>{{ consultation.dateConsultation ? consultation.dateConsultation|date('d/m/Y H:i') : '' }}</td>
            </tr>
            <tr>
                <th>Compte rendu</th>
                <td>{{ consultation.compteRendu }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-dark btn-primary mb-3" href="{{ path('app_consultation_index') }}">Retour à la liste</a>

    <a class="btn btn-outline-dark btn-primary mb-3" href="{{ path('app_consultation_edit', {'id': consultation.id}) }}">Modifier</a>

    {{ include('consultation/_delete_form.html.twig') }}
{% endblock %}
