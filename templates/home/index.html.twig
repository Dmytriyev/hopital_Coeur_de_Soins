{% extends 'base.html.twig' %}

{% block title %}Accueil
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>
		{% if app.user %}
			{# <h3>Bonjour
				{{ app.user.nom | capitalize }}
				{{ app.user.prenom | capitalize }} </h3> #}
                {# <h1>Hopital Coeur de Soins</h1> #}
		
		<div id="button-container" class=" align-items-center">
			<a class="button btn btn-outline-dark btn-primary" href="{{ path('app_user_new') }}">Ajouter un patient</a>
			<a class="button btn btn-outline-dark btn-primary" href="{{ path('app_dossier_medical_index') }}">Choisir un Dossier</a>
			<a class="button btn btn-outline-dark btn-primary" href="{{ path('app_rdv_new') }}">Prendre un RDV</a>
			<a class="button btn btn-outline-dark btn-primary" href="{{ path('app_user_index') }}">Choisir un Medecin ou un Patient</a>
		</div>
	{% else %}
		<h1>Bonjour, connectez vous</h1>
	{% endif %}
{% endblock %}
